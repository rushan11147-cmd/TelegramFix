ю
# 🔍 Диагностика проблемы "415 Unsupported Media Type"

## Что означает эта ошибка

Ошибка **415 Unsupported Media Type** означает, что сервер Flask не может обработать запрос, потому что:
1. Отсутствует заголовок `Content-Type: application/json`
2. Или Flask не настроен на прием JSON

## Проблема в вашем случае

Судя по скриншоту, проблема в том, что:
1. **Файл app.py обрезан** - функция `next_day()` не завершена (обрывается на строке `'passive_income': passive`)
2. Это приводит к синтаксической ошибке Python
3. Flask не может запуститься правильно

## Решение

### Шаг 1: Остановите все процессы Python

Откройте **Диспетчер задач** (Ctrl+Shift+Esc):
1. Найдите все процессы `python.exe`
2. Щелкните правой кнопкой → Завершить задачу
3. Закройте все окна командной строки

### Шаг 2: Проверьте файл app.py

Файл app.py должен быть полным. Последние строки должны выглядеть так:

```python
if __name__ == '__main__':
    # Для локальной разработки - только Flask без бота
    logger.info("🚀 Запуск в режиме разработки (только Flask, без Telegram бота)")
    logger.info("💡 Чтобы запустить бота, установите переменную окружения: RUN_BOT=true")
    port = int(os.environ.get('PORT', 5000))
    app.run(debug=False, host='0.0.0.0', port=port, use_reloader=False)
```

### Шаг 3: Исправьте файл

Если файл обрезан, нужно восстановить недостающую часть функции `next_day()`.

Найдите строку, которая заканчивается на:
```python
'passive_income': passive
```

И замените её на:
```python
'passive_income': passive_income,
'monthly_expenses': monthly_expenses,
'message': message,
'new_jobs': new_jobs
})
```

### Шаг 4: Запустите заново

```bash
python app.py
```

Откройте в браузере: http://127.0.0.1:5000

---

## Альтернативное решение

Если проблема сохраняется, попробуйте:

1. **Скачайте свежую версию app.py** с GitHub
2. Или **восстановите из бэкапа**
3. Или **используйте bot.py** для запуска только Telegram бота

---

## Как проверить, что файл правильный

Выполните:
```bash
python -c "import app; print('OK')"
```

Если увидите "OK" - файл правильный.
Если ошибка - файл поврежден.

---

## Нужна помощь?

Если ничего не помогает:
1. Закройте все окна
2. Перезагрузите компьютер
3. Запустите `start_flask_only.bat`
