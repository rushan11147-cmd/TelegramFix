# ✅ ИСПРАВЛЕНА ОШИБКА 415

## Что было исправлено

Проблема была в том, что все POST-маршруты Flask использовали `request.json` напрямую, что требовало заголовок `Content-Type: application/json`. Когда браузер делал запросы без этого заголовка, Flask возвращал ошибку 415.

## Изменения

Заменил во всех POST-маршрутах:
```python
data = request.json  # ❌ Старый код - требует Content-Type
```

На безопасный вариант:
```python
data = request.get_json(force=True, silent=True)  # ✅ Новый код - работает всегда
if not data:
    return jsonify({"error": "Invalid JSON data"}), 400
```

## Исправленные маршруты (15 штук)

1. `/api/set_name` - установка имени игрока
2. `/api/complete_tutorial` - завершение обучения
3. `/api/check_goals` - проверка целей
4. `/api/change_job` - смена работы
5. `/api/buy_booster` - покупка бустеров
6. `/api/buy_car` - покупка машины
7. `/api/buy_real_estate` - покупка недвижимости
8. `/api/select_trait` - выбор черты
9. `/api/buy_food` - покупка еды
10. `/api/take_rest` - отдых
11. `/api/random_event` - случайное событие
12. `/api/play_roulette` - рулетка
13. `/api/upgrade_skill` - прокачка навыков
14. `/api/work` - работа
15. `/api/next_day` - следующий день

Также исправлен **rate limiter**, который тоже использовал `request.json`.

## Как запустить

1. Остановите Flask (Ctrl+C в терминале)
2. Запустите заново:
   ```
   start_flask_only.bat
   ```
3. Откройте в браузере: http://127.0.0.1:8080

## Теперь должно работать! 🎉

Все маршруты теперь корректно обрабатывают JSON данные независимо от заголовков запроса.
