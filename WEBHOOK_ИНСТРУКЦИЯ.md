# Настройка Telegram бота через Webhook

## Проблема с polling решена!

Telegram бот не может работать с `polling` в отдельном потоке на Render. Теперь используется **webhook** - более надежный способ.

## Что изменилось

### Было (не работало):
- Бот пытался запуститься в отдельном потоке
- Использовал `run_polling()` 
- Ошибки: `set_wakeup_fd only works in main thread`

### Стало (работает):
- Бот работает через webhook
- Telegram отправляет сообщения на наш сервер
- Сервер отвечает через Telegram API

## Как активировать бота

### Шаг 1: Подожди деплоя
Render деплоит 2-3 минуты

### Шаг 2: Установи webhook
Открой в браузере:
```
https://telegramfix.onrender.com/set_webhook
```

Должен вернуть:
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

### Шаг 3: Проверь в Telegram
1. Открой бота в Telegram
2. Отправь `/start`
3. Должна появиться кнопка "🎮 Играть в 'Выживи до зарплаты'"
4. Нажми кнопку
5. Откроется игра! ✅

## Как это работает

```
Пользователь → /start
    ↓
Telegram → POST https://telegramfix.onrender.com/bot_webhook
    ↓
Наш сервер → обрабатывает команду
    ↓
Наш сервер → POST https://api.telegram.org/bot{TOKEN}/sendMessage
    ↓
Telegram → отправляет сообщение с кнопкой пользователю
```

## Проверка webhook

### Проверь что webhook установлен
Открой:
```
https://api.telegram.org/bot{ВАШ_ТОКЕН}/getWebhookInfo
```

Должно быть:
```json
{
  "ok": true,
  "result": {
    "url": "https://telegramfix.onrender.com/bot_webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Если webhook не работает

1. **Удали старый webhook:**
```
https://api.telegram.org/bot{ВАШ_ТОКЕН}/deleteWebhook
```

2. **Установи заново:**
```
https://telegramfix.onrender.com/set_webhook
```

3. **Проверь логи Render:**
Должно быть: `Webhook set result: {'ok': True, 'result': True}`

## Альтернатива

Если webhook не работает, можешь открыть игру напрямую:
```
https://telegramfix.onrender.com
```

Данные будут сохраняться в localStorage браузера.

## Важно!

- Webhook работает только с HTTPS (Render предоставляет HTTPS автоматически)
- Не нужно устанавливать `RUN_BOT=true` - webhook работает всегда
- Бот отвечает только на команду `/start`

---

**Дата:** 28 января 2026  
**Статус:** Готово к использованию
