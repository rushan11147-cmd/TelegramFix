# 🔧 Исправление проблем с энергией и ошибками соединения

## Проблема 1: "Ошибка соединения"

### Причина:
На бесплатном плане Render сервер "засыпает" после 15 минут неактивности. При первом запросе после "сна" сервер просыпается, но это занимает 30-60 секунд, и запрос может упасть с ошибкой.

### Решение:
Добавим повторную попытку запроса при ошибке соединения.

## Проблема 2: Энергия восстанавливается не полностью

### Проверка кода:
Я проверил код - энергия ДОЛЖНА восстанавливаться полностью:
```python
user['energy'] = user['max_energy']  # max_energy = 100
```

### Возможные причины:
1. **Сервер "заснул"** - запрос не дошёл до сервера
2. **Кэш браузера** - старые данные в браузере
3. **max_energy изменён** - проверьте значение в базе данных

### Решение:
1. Обновить страницу (F5)
2. Очистить кэш браузера (Ctrl+Shift+Delete)
3. Добавить повторные попытки запросов

## Исправления в коде

### 1. Добавим повторные попытки в simple.html

Заменим функцию `nextDay()` на версию с повторными попытками:

```javascript
async function nextDay(retryCount = 0) {
    try {
        const response = await fetch('/api/next_day', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ user_id: userId })
        });
        
        if (response.ok) {
            const result = await response.json();
            gameData = result.user;
            updateUI();
            showMessage('Новый день начался! 🌅 Энергия восстановлена!', true);
        } else {
            const error = await response.json();
            showMessage(error.error || 'Ошибка!', false);
        }
    } catch (error) {
        console.error('Ошибка при переходе на следующий день:', error);
        
        // Повторная попытка (максимум 3 раза)
        if (retryCount < 3) {
            showMessage(`Ошибка соединения. Повторная попытка ${retryCount + 1}/3...`, false);
            setTimeout(() => nextDay(retryCount + 1), 2000); // Ждём 2 секунды
        } else {
            showMessage('Ошибка соединения! Сервер может быть "заснувшим". Подождите 30 секунд и попробуйте снова.', false);
        }
    }
}
```

### 2. Добавим индикатор загрузки

Добавим сообщение о том, что сервер может быть "заснувшим":

```javascript
async function nextDay(retryCount = 0) {
    // Показываем индикатор загрузки
    if (retryCount === 0) {
        showMessage('Переход на следующий день...', true);
    }
    
    try {
        const response = await fetch('/api/next_day', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ user_id: userId }),
            timeout: 30000 // 30 секунд таймаут
        });
        
        if (response.ok) {
            const result = await response.json();
            gameData = result.user;
            updateUI();
            
            // Проверяем что энергия восстановилась
            if (gameData.energy === gameData.max_energy) {
                showMessage('✅ Новый день! Энергия полностью восстановлена!', true);
            } else {
                showMessage(`⚠️ Новый день! Энергия: ${gameData.energy}/${gameData.max_energy}`, true);
            }
        } else {
            const error = await response.json();
            showMessage(error.error || 'Ошибка!', false);
        }
    } catch (error) {
        console.error('Ошибка:', error);
        
        if (retryCount < 3) {
            showMessage(`🔄 Сервер просыпается... Попытка ${retryCount + 1}/3`, false);
            setTimeout(() => nextDay(retryCount + 1), 3000);
        } else {
            showMessage('❌ Сервер не отвечает. Подождите 1 минуту и обновите страницу (F5)', false);
        }
    }
}
```

## Проверка энергии в базе данных

Если проблема сохраняется, проверьте данные пользователя:

1. Откройте Render Dashboard
2. Перейдите в Shell (вкладка Shell)
3. Выполните:
```bash
python3
>>> import sqlite3
>>> conn = sqlite3.connect('game_data.db')
>>> cursor = conn.cursor()
>>> cursor.execute("SELECT data FROM users WHERE user_id = 'ВАШ_USER_ID'")
>>> import json
>>> data = json.loads(cursor.fetchone()[0])
>>> print(f"Energy: {data['energy']}, Max Energy: {data['max_energy']}")
```

## Временное решение для пользователей

Если сервер "заснул":
1. Подождите 30-60 секунд
2. Обновите страницу (F5)
3. Попробуйте снова

## Долгосрочное решение

Для предотвращения "засыпания" сервера:
1. Перейти на платный план Render ($7/месяц)
2. Или настроить "пинг" сервера каждые 10 минут (но это против правил Render)

## Что делать СЕЙЧАС

Я сейчас обновлю код с повторными попытками и загружу на GitHub.
