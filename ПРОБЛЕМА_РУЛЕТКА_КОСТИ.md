# Проблема с рулеткой и костями

## Что не работает
Рулетка и кости в системе развлечений не работают.

## Проверка кода

### ✅ Backend код правильный
1. `entertainment_system.py` - RouletteEngine и DiceEngine реализованы
2. `app.py` - API endpoints `/api/entertainment/roulette` и `/api/entertainment/dice` существуют
3. Логика игр корректна

### ✅ Frontend код правильный
1. JavaScript функции `playRoulette()` и `playDice()` реализованы
2. Правильные API endpoints используются
3. Анимация и обработка результатов есть

## Возможные причины

### 1. Сервер не запущен
Проверьте, запущен ли Flask сервер:
```bash
python app.py
```

### 2. Ошибка в консоли браузера
Откройте консоль браузера (F12 → Console) и проверьте ошибки при нажатии на кнопки рулетки/костей.

### 3. API возвращает ошибку
Проверьте логи сервера Flask - там должны быть запросы к `/api/entertainment/roulette` и `/api/entertainment/dice`.

### 4. Проблема с балансом
Убедитесь, что у пользователя достаточно денег для ставки:
- Рулетка: минимум 50₽
- Кости: минимум 100₽

### 5. Проблема с user_id
Проверьте, что `userId` правильно передается в JavaScript.

## Как протестировать

### 1. Проверить API напрямую

Откройте консоль браузера (F12) и выполните:

```javascript
// Тест рулетки
fetch('/api/entertainment/roulette', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ user_id: 'test123', bet: 100 })
})
.then(r => r.json())
.then(console.log)
.catch(console.error);

// Тест костей
fetch('/api/entertainment/dice', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ user_id: 'test123', bet: 100, choice: 'low' })
})
.then(r => r.json())
.then(console.log)
.catch(console.error);
```

### 2. Проверить логи сервера

После нажатия на кнопку рулетки/костей в игре, проверьте вывод Flask сервера. Должны быть строки:
```
INFO - Playing roulette: user=..., bet=...
INFO - Playing dice: user=..., bet=..., choice=...
```

### 3. Проверить ответ API

Если API работает, ответ должен быть:
```json
{
  "success": true,
  "result": {
    "reels": ["🍒", "🍒", "🍒"],  // для рулетки
    "dice1": 3, "dice2": 4,        // для костей
    "won": true,
    "payout": 200
  },
  "message": "Выигрыш!",
  "user": { ... }
}
```

## Быстрое исправление

Если проблема в том, что кнопки не реагируют:

1. Откройте `templates/simple.html`
2. Найдите функции `playRoulette()` и `playDice()`
3. Добавьте в начало функций:
```javascript
console.log('playRoulette called, bet:', bet);
console.log('playDice called, choice:', choice, 'bet:', bet);
```

4. Обновите страницу и нажмите на кнопки
5. Проверьте консоль браузера - должны появиться эти сообщения

## Что делать дальше

1. Запустите сервер: `python app.py`
2. Откройте игру в браузере
3. Откройте консоль браузера (F12)
4. Нажмите на кнопку "Игры" → "Рулетка" или "Кости"
5. Посмотрите на ошибки в консоли
6. Покажите мне ошибки - я помогу исправить

## Типичные ошибки

### "Недостаточно денег"
Убедитесь, что у пользователя есть деньги. Добавьте денег через консоль:
```javascript
gameData.money = 10000;
updateUI();
```

### "user_id is required"
Проблема с передачей userId. Проверьте, что `userId` определен в JavaScript.

### "Failed to fetch"
Сервер не запущен или недоступен. Запустите `python app.py`.

### Кнопки не реагируют
Проверьте, что модальное окно открывается. Если нет - проблема в функции `showEntertainment()`.
