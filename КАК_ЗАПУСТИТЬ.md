# 🚀 Как запустить проект

## 📋 Варианты запуска

### 1️⃣ Только Flask (БЕЗ Telegram бота) - РЕКОМЕНДУЕТСЯ для разработки

Просто запустите:
```bash
python app.py
```

Или двойной клик на файл:
```
start_flask_only.bat
```

**Что будет работать:**
- ✅ Веб-интерфейс на http://127.0.0.1:5000
- ✅ Все API endpoints
- ✅ База данных
- ❌ Telegram бот НЕ запустится (чтобы избежать конфликтов)

---

### 2️⃣ Flask + Telegram бот

Двойной клик на файл:
```
start_with_bot.bat
```

Или через командную строку:
```bash
set RUN_BOT=true
python app.py
```

**Что будет работать:**
- ✅ Веб-интерфейс на http://127.0.0.1:5000
- ✅ Все API endpoints
- ✅ База данных
- ✅ Telegram бот

**⚠️ ВАЖНО:** Убедитесь, что нет других запущенных экземпляров бота!

---

### 3️⃣ Только Telegram бот (БЕЗ Flask)

```bash
python bot.py
```

---

## 🔧 Решение проблем

### Ошибка "Conflict: terminated by other getUpdates request"

**Причина:** Запущено несколько экземпляров бота одновременно.

**Решение:**
1. Закройте все окна с запущенным приложением
2. Откройте Диспетчер задач (Ctrl+Shift+Esc)
3. Найдите все процессы `python.exe` и завершите их
4. Запустите приложение заново

### Веб-интерфейс не открывается

1. Проверьте, что приложение запущено
2. Откройте браузер и перейдите на http://127.0.0.1:5000
3. Проверьте, что порт 5000 не занят другим приложением

### Telegram бот не отвечает

1. Проверьте, что в `.env` файле указан правильный `TELEGRAM_BOT_TOKEN`
2. Убедитесь, что запущен вариант с ботом (`start_with_bot.bat`)
3. Проверьте логи на наличие ошибок

---

## 📦 Production (Render.com)

На Render.com бот запускается автоматически через `Procfile`:
```
web: RUN_BOT=true gunicorn app:app
```

Переменная `RUN_BOT=true` включает запуск Telegram бота вместе с Flask.

---

## 💡 Рекомендации

- **Для разработки:** Используйте `start_flask_only.bat` (без бота)
- **Для тестирования бота:** Используйте `start_with_bot.bat`
- **Для production:** Деплойте на Render.com через Git

---

## 🆘 Нужна помощь?

Если ничего не работает:
1. Остановите все процессы Python
2. Удалите файл `game_data.db`
3. Запустите `start_flask_only.bat`
4. Откройте http://127.0.0.1:5000 в браузере
