# Инструкция по деплою исправлений безопасности

## ✅ Что было исправлено

1. Убран глобальный кэш - нет race conditions
2. Добавлена валидация user_id
3. Защита от отрицательных денег
4. Rate limiting на все POST эндпоинты
5. Лимит на количество кредитов (10 макс)
6. Функция clamp() для ограничения значений

## 🚀 Деплой на Render

### Автоматический деплой
Render автоматически задеплоит изменения из GitHub.

Проверь статус:
1. Зайди на https://dashboard.render.com
2. Открой сервис "TelegramFix"
3. Дождись завершения деплоя (2-3 минуты)

### Проверка после деплоя

1. **Проверь health check:**
   ```
   https://telegramfix.onrender.com/health
   ```
   Должно быть: `{"database":"PostgreSQL","status":"healthy"}`

2. **Проверь что игра работает:**
   - Открой игру в Telegram
   - Попробуй поработать
   - Обнови страницу - прогресс должен сохраниться

3. **Проверь логи на Render:**
   - Не должно быть ошибок
   - Должны быть сообщения "Using PostgreSQL database"

## ⚠️ Возможные проблемы

### Проблема 1: Ошибка "Invalid user_id"
**Причина:** Новая валидация user_id

**Решение:** Это нормально - защита от некорректных ID

### Проблема 2: 429 Too Many Requests
**Причина:** Rate limiting

**Решение:** Это нормально - защита от спама. Подожди минуту.

### Проблема 3: Медленная работа
**Причина:** Теперь каждый запрос читает из БД

**Решение:** PostgreSQL быстрый, но если проблема - можно добавить Redis кэш

## 📊 Мониторинг

### Что отслеживать:

1. **Логи ошибок:**
   ```
   "Preventing negative money save"
   "Invalid user_id"
   "User has too many credits"
   ```

2. **Производительность:**
   - Время ответа эндпоинтов
   - Количество запросов к БД

3. **Rate limiting:**
   - Количество 429 ошибок
   - Если много - увеличить лимиты

## 🔄 Откат изменений (если что-то сломалось)

### Быстрый откат:

1. На Render → TelegramFix → Manual Deploy
2. Выбери предыдущий коммит `f0e91b9`
3. Deploy

### Или через Git:

```bash
git revert 91f9029
git push
```

## 📝 Следующие шаги

После успешного деплоя:

1. ✅ Протестировать все функции игры
2. ✅ Проверить что прогресс сохраняется
3. ✅ Мониторить логи 24 часа
4. ⏳ Реализовать оставшиеся исправления (атомарные операции)

## 🎯 Оценка улучшений

**Безопасность:**
- Было: 4/10
- Стало: 7/10 ⬆️

**Стабильность:**
- Было: 6/10 (потеря прогресса)
- Стало: 9/10 ⬆️

**Производительность:**
- Было: 8/10
- Стало: 7/10 ⬇️ (больше запросов к БД, но PostgreSQL быстрый)

---

**Дата:** 28 января 2026  
**Версия:** 1.0
