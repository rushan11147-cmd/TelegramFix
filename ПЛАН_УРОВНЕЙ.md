# 📊 План реализации системы уровней

## Концепция:

Игрок проходит 3 уровня жизни:
1. **Уровень 1 - Бедный** (0 - 100,000₽)
2. **Уровень 2 - Средний класс** (100,000 - 500,000₽)
3. **Уровень 3 - Богатый** (500,000₽+)

---

## Что нужно добавить в backend:

### 1. Система уровней в `app.py`:

```python
# Уровни игрока
LEVELS = {
    1: {
        "name": "Обычный уровень",
        "min_money": 0,
        "max_money": 100000,
        "image": "/static/images/level1.jpg",
        "description": "Начало пути",
        "unlocks": ["delivery"]
    },
    2: {
        "name": "Средний уровень",
        "min_money": 100000,
        "max_money": 500000,
        "image": "/static/images/level2.jpg",
        "description": "Стабильность",
        "unlocks": ["office", "old_car"]
    },
    3: {
        "name": "Высокий уровень",
        "min_money": 500000,
        "max_money": float('inf'),
        "image": "/static/images/level3.jpg",
        "description": "Успех",
        "unlocks": ["freelance", "crypto", "luxury_car"]
    }
}

def get_player_level(money):
    """Определить уровень игрока по деньгам"""
    for level_id, level_data in LEVELS.items():
        if level_data['min_money'] <= money < level_data['max_money']:
            return level_id
    return 3  # Максимальный уровень
```

### 2. Добавить в данные пользователя:

```python
'level': 1,  # Текущий уровень
'total_earned': 0,  # Всего заработано за игру
'level_progress': 0,  # Прогресс до следующего уровня (%)
```

### 3. API endpoint для получения уровня:

```python
@app.route('/api/level/<user_id>')
def get_level(user_id):
    if user_id not in users_data:
        return jsonify({"error": "User not found"}), 404
    
    user = users_data[user_id]
    level = get_player_level(user['money'])
    level_data = LEVELS[level]
    
    # Рассчитать прогресс
    if level < 3:
        progress = ((user['money'] - level_data['min_money']) / 
                   (level_data['max_money'] - level_data['min_money'])) * 100
    else:
        progress = 100
    
    return jsonify({
        'level': level,
        'level_data': level_data,
        'progress': progress
    })
```

---

## Что нужно в frontend:

### 1. Карточки уровней:

```html
<div class="levels-container">
    <div class="level-card" data-level="1">
        <img src="/static/images/level1.jpg" alt="Уровень 1">
        <div class="level-info">
            <div class="level-number">1</div>
            <div class="level-name">Обычный уровень</div>
        </div>
    </div>
    
    <div class="level-card active" data-level="2">
        <img src="/static/images/level2.jpg" alt="Уровень 2">
        <div class="level-info">
            <div class="level-number">2</div>
            <div class="level-name">Средний уровень</div>
        </div>
    </div>
    
    <div class="level-card locked" data-level="3">
        <img src="/static/images/level3.jpg" alt="Уровень 3">
        <div class="level-info">
            <div class="level-number">3</div>
            <div class="level-name">Высокий уровень</div>
        </div>
    </div>
</div>
```

### 2. CSS для карточек:

```css
.levels-container {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    padding: 10px;
}

.level-card {
    min-width: 200px;
    border-radius: 15px;
    overflow: hidden;
    position: relative;
    border: 3px solid #8b7355;
}

.level-card.active {
    border-color: #27ae60;
    box-shadow: 0 0 20px rgba(39,174,96,0.5);
}

.level-card.locked {
    opacity: 0.5;
    filter: grayscale(100%);
}

.level-card img {
    width: 100%;
    height: 250px;
    object-fit: cover;
}
```

---

## Этапы реализации:

### Этап 1: Подготовка (СЕЙЧАС)
- ✅ Создать инструкцию по изображениям
- ✅ Создать план реализации
- ⏳ Найти/создать изображения

### Этап 2: Backend
- [ ] Добавить систему уровней в app.py
- [ ] Создать API endpoint /api/level
- [ ] Обновить данные пользователя

### Этап 3: Frontend
- [ ] Создать HTML структуру с карточками
- [ ] Добавить CSS стили
- [ ] Подключить JavaScript логику
- [ ] Добавить анимации переходов

### Этап 4: Интеграция
- [ ] Связать уровни с открытием контента
- [ ] Добавить уведомления о повышении уровня
- [ ] Тестирование

---

## Временное решение (без изображений):

Пока нет изображений, используем:

```html
<div class="level-card">
    <div class="level-placeholder">
        <div class="level-emoji">🚴</div>
    </div>
    <div class="level-info">
        <div class="level-number">1</div>
        <div class="level-name">Обычный уровень</div>
    </div>
</div>
```

```css
.level-placeholder {
    width: 100%;
    height: 250px;
    background: linear-gradient(135deg, #8b7355 0%, #654321 100%);
    display: flex;
    align-items: center;
    justify-content: center;
}

.level-emoji {
    font-size: 80px;
}
```

---

## Следующие шаги:

1. **Найди изображения** (см. ИНСТРУКЦИЯ_ИЗОБРАЖЕНИЯ.md)
2. **Сохрани в** `static/images/`
3. **Скажи мне** когда будут готовы
4. **Я добавлю** систему уровней в код

Или можем начать с временного решения (эмодзи) прямо сейчас!

---

**Что выбираешь?**
- A) Ждем изображения, потом делаем
- B) Делаем сейчас с эмодзи, потом заменим на изображения

