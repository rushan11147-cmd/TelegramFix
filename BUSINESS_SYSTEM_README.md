# 🏪 Система бизнеса - Документация

## ✅ Что реализовано

### Backend (Python/Flask)

1. **Модели данных** (`business_system.py`):
   - Business, Employee, Upgrade, BusinessEvent
   - Enums: BusinessType, EmployeeType, UpgradeType, EventType
   - Конфигурации: BUSINESS_CONFIGS, EMPLOYEE_CONFIGS, UPGRADE_CONFIGS, EVENT_CONFIGS

2. **Менеджеры**:
   - `EmployeeManager` - найм/увольнение сотрудников
   - `InventoryManager` - управление запасами
   - `UpgradeManager` - покупка улучшений
   - `EventManager` - случайные события
   - `RevenueCalculator` - расчет доходов/расходов
   - `BusinessManager` - главный оркестратор
   - `BusinessRepository` - работа с БД

3. **API Endpoints** (интегрированы в `app.py`):
   - `POST /api/business/create` - создать бизнес
   - `GET /api/business/list` - список бизнесов
   - `GET /api/business/<id>` - детали бизнеса
   - `POST /api/business/<id>/hire` - нанять сотрудника
   - `POST /api/business/<id>/fire/<employee_id>` - уволить
   - `POST /api/business/<id>/buy-inventory` - купить запасы
   - `POST /api/business/<id>/upgrade` - купить улучшение
   - `POST /api/business/<id>/sell` - продать бизнес
   - `POST /api/business/<id>/repair` - отремонтировать
   - `GET /api/business/configs` - получить конфигурации

4. **Интеграция с игрой**:
   - Ежедневная обработка бизнесов при переходе дня
   - Автоматический расчет доходов/расходов
   - Случайные события
   - Достижения (Businessman, Tycoon)

## 📊 Типы бизнеса

| Тип | Стоимость | Доход/день | Расходы/день | Макс. сотрудников |
|-----|-----------|------------|--------------|-------------------|
| 🏪 Киоск | 50,000₽ | 8,000₽ | 2,000₽ | 2 |
| ☕ Кафе | 300,000₽ | 40,000₽ | 10,000₽ | 4 |
| 🍽️ Ресторан | 1,000,000₽ | 150,000₽ | 30,000₽ | 6 |
| 🏢 Сеть | 5,000,000₽ | 800,000₽ | 150,000₽ | 10 |

## 👥 Сотрудники

| Тип | Зарплата/день | Эффект |
|-----|---------------|--------|
| 👨‍🍳 Повар | 5,000₽ | +30% качество, +0.5 рейтинг |
| 💰 Кассир | 3,000₽ | +20% скорость обслуживания |
| 👔 Менеджер | 8,000₽ | +25% к доходу |

## ⬆️ Улучшения

| Улучшение | Стоимость | Эффект | Тип |
|-----------|-----------|--------|-----|
| 📋 Новое меню | 50,000₽ | +30% доход | Постоянное |
| 🚚 Доставка | 80,000₽ | +50% доход | Постоянное |
| 🔨 Ремонт | 100,000₽ | +1 звезда | Постоянное |
| 📢 Реклама | 30,000₽ | +20% клиентов | 7 дней |

## 🎲 События

- 🏥 Проверка санэпидемстанции (5% шанс) - штраф или закрытие
- 🏪 Открылся конкурент (3% шанс) - -20% дохода на 14 дней
- 📱 Вирусный пост (2% шанс) - +50% дохода на 3 дня
- ⚙️ Поломка оборудования (4% шанс) - требует ремонт 20,000₽

## 💰 Формула расчета дохода

```
Базовый доход = base_revenue (зависит от типа)
Множитель рейтинга = rating / 3.0
Доход = Базовый доход × Множитель рейтинга

Применяются множители:
× Менеджер (1.25x)
× Новое меню (1.30x)
× Доставка (1.50x)
× Реклама (1.20x)
× События (0.8x - 1.5x)
× Низкие запасы (0.5x если < 20%)
```

## 📦 Запасы

- Начальный уровень: 100%
- Расход: 10% в день
- Пополнение: 5,000₽ за 50%
- Штраф: -50% дохода если < 20%
- Штраф рейтинга: -0.5 звезды если < 20% три дня подряд

## 🎯 Достижения

- **Businessman** 💼 - заработать 100,000₽ чистой прибыли
- **Tycoon** 👑 - владеть сетью ресторанов

## 🔧 Как использовать

### Создание бизнеса

```javascript
fetch('/api/business/create', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    user_id: 'user123',
    business_type: 'kiosk'  // kiosk, cafe, restaurant, restaurant_chain
  })
})
```

### Получение списка бизнесов

```javascript
fetch('/api/business/list?user_id=user123')
  .then(r => r.json())
  .then(data => {
    console.log(data.businesses); // Массив бизнесов со статистикой
  })
```

### Найм сотрудника

```javascript
fetch('/api/business/BUSINESS_ID/hire', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    user_id: 'user123',
    employee_type: 'chef'  // chef, cashier, manager
  })
})
```

### Покупка запасов

```javascript
fetch('/api/business/BUSINESS_ID/buy-inventory', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    user_id: 'user123'
  })
})
```

### Покупка улучшения

```javascript
fetch('/api/business/BUSINESS_ID/upgrade', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    user_id: 'user123',
    upgrade_type: 'new_menu'  // new_menu, delivery, renovation, advertising
  })
})
```

## 📝 Следующие шаги

### Для полной реализации нужно:

1. **Frontend UI** - создать интерфейс для управления бизнесами
2. **Тесты** - написать property-based и unit тесты
3. **Балансировка** - протестировать и настроить экономику
4. **Визуализация** - добавить графики доходов/расходов

### Рекомендуемый порядок:

1. Создать простой UI для списка бизнесов
2. Добавить кнопки для основных действий
3. Протестировать создание и управление бизнесом
4. Добавить визуализацию событий
5. Написать тесты

## 🐛 Отладка

Проверить работу системы:

```python
# В Python консоли
from business_system import *
from app import business_manager

# Создать тестовый бизнес
result = business_manager.create_business('test_user', BusinessType.KIOSK)
print(result.success, result.data)

# Получить список бизнесов
businesses = business_manager.get_user_businesses('test_user')
print(len(businesses))

# Обработать день
report = business_manager.process_daily_operations('test_user')
print(f"Прибыль: {report.total_net_profit}")
```

## 📊 Статус реализации

✅ Модели данных
✅ Бизнес-логика
✅ API endpoints
✅ Интеграция с игрой
⏳ Frontend UI (не реализовано)
⏳ Тесты (не реализовано)
⏳ Балансировка (требуется)

---

**Система готова к использованию!** Осталось только создать UI для взаимодействия с API.
