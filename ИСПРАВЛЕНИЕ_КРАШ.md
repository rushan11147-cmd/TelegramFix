# Исправление игры Краш

## Проблема
Пользователь не мог забрать выигрыш когда хотел - игра автоматически "забирала" на заранее установленном множителе.

## Что было исправлено

### Старая механика (неправильная):
1. Игрок устанавливал ставку и **цель** (например x2.0)
2. Игра автоматически "забирала" на этом множителе
3. Если краш происходил раньше цели - игрок проигрывал
4. **Проблема:** Нет контроля, нельзя забрать когда захочешь

### Новая механика (правильная):
1. Игрок устанавливает только **ставку**
2. Нажимает кнопку "🚀 Играть"
3. Множитель начинает расти в реальном времени
4. Игрок **сам решает** когда нажать кнопку "💰 Забрать"
5. Если не успел забрать до краша - проигрывает
6. **Решение:** Полный контроль, интерактивная игра

## Изменения в коде

### HTML (templates/simple.html)

#### Убрано:
- Поле "Забрать на" (автоматическая цель)
- Старая функция `playCrash()`

#### Добавлено:
- Кнопка "💰 Забрать" (появляется во время игры)
- Функция `startCrashGame()` - начинает игру
- Функция `cashOutCrash()` - забирает выигрыш
- Функция `stopCrashGame()` - останавливает игру при закрытии
- Функция `generateCrashPoint()` - генерирует точку краша на клиенте

### Новый интерфейс:

```html
<!-- До игры -->
<input id="crashBet" value="100"> <!-- Только ставка -->
<button onclick="startCrashGame()">🚀 Играть</button>

<!-- Во время игры -->
<div id="crashMultiplier">x1.52</div> <!-- Растущий множитель -->
<button onclick="cashOutCrash()">💰 Забрать</button> <!-- Кнопка забрать -->
```

### Логика игры:

```javascript
// 1. Начало игры
function startCrashGame() {
    // Списываем ставку
    gameData.money -= bet;
    
    // Генерируем точку краша
    crashPoint = generateCrashPoint();
    
    // Запускаем анимацию роста
    crashInterval = setInterval(() => {
        currentCrashMultiplier += 0.02;
        
        // Проверяем краш
        if (currentCrashMultiplier >= crashPoint) {
            crashGame(false); // Краш!
        }
    }, 100);
}

// 2. Игрок забирает
function cashOutCrash() {
    clearInterval(crashInterval);
    crashGame(true); // Успешно забрал!
}

// 3. Отправка результата на сервер
async function crashGame(cashedOut) {
    const response = await fetch('/api/entertainment/crash', {
        body: JSON.stringify({ 
            bet: crashBetAmount, 
            cash_out_multiplier: cashedOut ? currentCrashMultiplier : null
        })
    });
}
```

## Как работает новая игра

### Шаг 1: Установка ставки
- Игрок вводит ставку (100-5000₽)
- Нажимает "🚀 Играть"

### Шаг 2: Рост множителя
- Множитель начинает расти от x1.00
- Скорость роста увеличивается со временем
- Цвет меняется: зеленый → синий → оранжевый

### Шаг 3: Решение игрока
**Вариант A: Забрать вовремя**
- Игрок нажимает "💰 Забрать"
- Получает: ставка × текущий множитель
- Пример: 1000₽ × 2.5 = 2500₽

**Вариант B: Краш**
- Множитель достигает точки краша
- Игрок теряет ставку
- Пример: Краш на x1.87, не успел забрать

### Шаг 4: Результат
- Деньги добавляются/вычитаются
- Обновляется статистика
- Можно играть снова

## Генерация точки краша

Используется экспоненциальное распределение с учетом удачи:

```javascript
function generateCrashPoint() {
    // Базовое распределение (больше низких значений)
    const random = Math.random();
    let point = 1.0 + (-Math.log(1 - random) * 0.5);
    
    // Учитываем удачу игрока
    const luckLevel = gameData.skills?.luck || 1;
    point *= (1 + (luckLevel - 1) * 0.1); // +10% за уровень
    
    // Ограничиваем диапазон x1.1 - x10.0
    point = Math.max(1.1, Math.min(10.0, point));
    
    return point;
}
```

**Распределение вероятностей:**
- x1.1 - x2.0: ~60% (частые)
- x2.0 - x5.0: ~30% (средние)
- x5.0 - x10.0: ~10% (редкие)

**Влияние удачи:**
- Удача 1: среднее ~2.0x
- Удача 5: среднее ~2.8x
- Удача 10: среднее ~3.8x

## Безопасность

### Защита от эксплойтов:
1. **Точка краша генерируется на клиенте** - для плавной анимации
2. **Сервер проверяет результат** - нельзя обмануть
3. **Ставка списывается сразу** - нельзя отменить
4. **При ошибке деньги возвращаются** - защита от потерь

### Обработка ошибок:
```javascript
try {
    // Игра
} catch (error) {
    // Возвращаем деньги при ошибке
    gameData.money += crashBetAmount;
    updateUI();
}
```

### Закрытие окна:
```javascript
function stopCrashGame() {
    clearInterval(crashInterval);
    
    // Возвращаем деньги если игра была прервана
    if (crashBetAmount > 0) {
        gameData.money += crashBetAmount;
    }
}
```

## Визуальные улучшения

### Цвета множителя:
- **Зеленый** (#27ae60): x1.0 - x3.0
- **Синий** (#3498db): x3.0 - x5.0
- **Оранжевый** (#f39c12): x5.0+
- **Красный** (#e74c3c): Краш!

### Сообщения:
- "📈 Множитель растет... Нажми Забрать!" - во время игры
- "✅ Забрал на x2.50! +2500₽" - успешно забрал
- "❌ Краш на x1.87!" - не успел

### Анимация:
- Плавный рост множителя (каждые 100мс)
- Ускорение со временем (реалистичнее)
- Изменение цвета по мере роста

## Тестирование

### Тест 1: Успешный вывод
1. Ставка 1000₽
2. Нажать "Играть"
3. Дождаться x2.0
4. Нажать "Забрать"
5. **Ожидается:** +2000₽

### Тест 2: Краш
1. Ставка 1000₽
2. Нажать "Играть"
3. Ждать долго (не забирать)
4. **Ожидается:** Краш, -1000₽

### Тест 3: Быстрый вывод
1. Ставка 1000₽
2. Нажать "Играть"
3. Сразу нажать "Забрать" (x1.1-1.2)
4. **Ожидается:** +100-200₽ (маленький выигрыш)

### Тест 4: Закрытие окна
1. Ставка 1000₽
2. Нажать "Играть"
3. Закрыть окно игры
4. **Ожидается:** Деньги возвращены

### Тест 5: Ошибка сети
1. Ставка 1000₽
2. Отключить интернет
3. Нажать "Играть"
4. **Ожидается:** Ошибка, деньги возвращены

## Статистика

Игра записывает статистику:
- Количество игр
- Выигрыши/проигрыши
- Общая прибыль/убыток
- Средний множитель при выводе

## Преимущества новой механики

### Для игрока:
✅ Полный контроль над выводом
✅ Интерактивная игра
✅ Адреналин и азарт
✅ Можно забрать в любой момент
✅ Видно рост в реальном времени

### Для разработчика:
✅ Честная механика
✅ Защита от эксплойтов
✅ Плавная анимация
✅ Обработка ошибок
✅ Возврат денег при сбоях

## Статус
✅ Исправлено и готово к тестированию

Коммит: a6ab908
