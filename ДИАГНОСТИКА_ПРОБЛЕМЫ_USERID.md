# Диагностика проблемы с userId

## Найденная проблема

**Причина сброса данных:** `userId` не сохранялся между обновлениями страницы!

### Что было не так

```javascript
// БЫЛО (неправильно):
let userId = 'demo_user';  // ❌ Всегда одинаковый для всех

// При открытии через Telegram:
userId = user.id.toString();  // ✅ Уникальный

// НО при обновлении страницы:
userId = 'demo_user';  // ❌ Снова сбрасывается!
```

### Что происходило

1. Пользователь открывает игру → `userId = 'demo_user'`
2. Выбирает черту → сохраняется для `'demo_user'`
3. **Обновляет страницу F5** → `userId = 'demo_user'` (новый!)
4. Загружается `/api/user/demo_user` → создается НОВЫЙ пользователь
5. Все данные сброшены! 😭

## Решение

### 1. Сохранение userId в localStorage

```javascript
// СТАЛО (правильно):
let userId = localStorage.getItem('userId') || 'demo_user_' + Math.random().toString(36).substr(2, 9);

// При открытии через Telegram:
userId = 'tg_' + user.id.toString();
localStorage.setItem('userId', userId);  // ✅ Сохраняем!

// При обновлении страницы:
userId = localStorage.getItem('userId');  // ✅ Загружаем сохраненный!
```

### 2. Добавлено логирование

В `app.py` добавлены логи:
- При загрузке пользователя: `Loading user data for: {user_id}`
- При сохранении: `Saving user data for: {user_id} - money: X, trait: Y`
- При создании нового: `Creating new user: {user_id}`

## Как это работает теперь

### Сценарий 1: Первый запуск
1. Открываешь игру → генерируется `userId = 'demo_user_abc123'`
2. Сохраняется в localStorage
3. Выбираешь черту → сохраняется для `'demo_user_abc123'`
4. **Обновляешь страницу** → загружается `'demo_user_abc123'` из localStorage
5. Данные загружаются! ✅

### Сценарий 2: Через Telegram
1. Открываешь через Telegram → `userId = 'tg_123456789'`
2. Сохраняется в localStorage
3. Играешь → все сохраняется для `'tg_123456789'`
4. **Обновляешь страницу** → загружается `'tg_123456789'` из localStorage
5. Данные загружаются! ✅

## Проверка после деплоя

### 1. Открой консоль браузера (F12)
Должно быть:
```
User ID: demo_user_abc123
```
(или `tg_123456789` если через Telegram)

### 2. Проверь localStorage
В консоли:
```javascript
localStorage.getItem('userId')
```
Должно вернуть твой userId

### 3. Тест сохранения
1. Выбери черту характера
2. Обнови страницу F5
3. Проверь консоль - должен быть тот же userId
4. Черта должна остаться! ✅

### 4. Проверь логи Render
После обновления страницы в логах должно быть:
```
Loading user data for: demo_user_abc123
User demo_user_abc123 found in DB - money: 500, trait: терпила
```

## Если проблема осталась

### Очисти localStorage
В консоли браузера:
```javascript
localStorage.clear()
location.reload()
```

Это создаст новый userId и начнет заново.

### Проверь логи Render
Ищи строки:
- `Creating new user:` - создается новый пользователь (плохо если часто)
- `User X found in DB` - пользователь найден (хорошо)
- `Invalid user_id:` - невалидный userId (проблема)

## Время деплоя
Render деплоит за 2-3 минуты. Подожди и проверь!

---

**Дата:** 28 января 2026  
**Деплой:** #3 (Критический - userId)  
**Статус:** Готово к проверке
