# 🔄 СБРОС БАЗЫ ДАННЫХ ЧЕРЕЗ API (БЕСПЛАТНО!)

## ✅ ЧТО ДОБАВЛЕНО

1. **API Endpoint**: `/api/admin/reset_database`
2. **Админ страница**: `/admin/reset`
3. **Пароль**: `ADMIN_PASSWORD` в .env

## 🚀 КАК ИСПОЛЬЗОВАТЬ

### СПОСОБ 1: Через веб-интерфейс (САМЫЙ ПРОСТОЙ)

#### Локально:
1. Открой http://127.0.0.1:8080/admin/reset
2. Введи пароль: `admin123`
3. Нажми "Сбросить базу данных"
4. Подтверди 2 раза

#### На продакшене (после деплоя):
1. Открой https://telegramfix.onrender.com/admin/reset
2. Введи пароль: `admin123`
3. Нажми "Сбросить базу данных"
4. Подтверди 2 раза

### СПОСОБ 2: Через curl (для продвинутых)

#### Локально:
```bash
curl -X POST http://127.0.0.1:8080/api/admin/reset_database \
  -H "Content-Type: application/json" \
  -d "{\"password\":\"admin123\"}"
```

#### На продакшене:
```bash
curl -X POST https://telegramfix.onrender.com/api/admin/reset_database \
  -H "Content-Type: application/json" \
  -d "{\"password\":\"admin123\"}"
```

### СПОСОБ 3: Через Postman

1. Метод: POST
2. URL: https://telegramfix.onrender.com/api/admin/reset_database
3. Headers: Content-Type: application/json
4. Body (raw JSON):
```json
{
  "password": "admin123"
}
```

## 🔐 БЕЗОПАСНОСТЬ

### Изменить пароль:

1. **Локально** - в файле `.env`:
```
ADMIN_PASSWORD=твой_секретный_пароль
```

2. **На Render**:
   - Dashboard → твой сервис
   - Environment → Add Environment Variable
   - Key: `ADMIN_PASSWORD`
   - Value: `твой_секретный_пароль`
   - Save Changes

⚠️ **ВАЖНО**: Не используй простой пароль на продакшене!

Примеры хороших паролей:
- `MyS3cr3tP@ssw0rd!2024`
- `R3nd3r_Adm1n_K3y_9876`
- `T3l3gr@m_G@m3_R3s3t_2024`

## 📋 ЧТО ПРОИЗОЙДЕТ

После сброса:
- ✅ Все пользователи удалены из базы
- ✅ При следующем входе создастся новый профиль
- ✅ Все начнут с 500₽, день 1, энергия 100

## 🧪 ТЕСТИРОВАНИЕ

### Локально (СЕЙЧАС):
1. Открой http://127.0.0.1:8080/admin/reset
2. Введи пароль: `admin123`
3. Сбрось базу
4. Открой http://127.0.0.1:8080
5. Проверь что начался новый профиль

### На продакшене (после деплоя):
1. Дождись деплоя (~5 минут)
2. Открой https://telegramfix.onrender.com/admin/reset
3. Введи пароль: `admin123`
4. Сбрось базу
5. Открой https://telegramfix.onrender.com
6. Проверь что начался новый профиль

## 📝 ДЕПЛОЙ

Чтобы задеплоить эти изменения:

```bash
git add app.py templates/admin_reset.html .env
git commit -m "feat: добавлен API для сброса базы данных"
git push origin main
```

⚠️ **НЕ ЗАБУДЬ** добавить `ADMIN_PASSWORD` в Environment Variables на Render!

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Пароль в .env** - только для локальной разработки
2. **На Render** - установи пароль через Environment Variables
3. **Не коммить** реальный пароль в Git!
4. **Сброс необратим** - все данные будут потеряны!

## 🎯 ГОТОВО!

Теперь ты можешь сбрасывать базу данных бесплатно через веб-интерфейс! 🎉
