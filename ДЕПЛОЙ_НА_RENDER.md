# 🚀 Деплой игры на Render

## Что нужно сделать

### 1️⃣ Загрузить код на GitHub

Если ещё не загрузили:

```bash
git add .
git commit -m "Исправлена ошибка 415, готово к деплою"
git push origin main
```

### 2️⃣ Создать Web Service на Render

1. Зайдите на https://render.com
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий с игрой

### 3️⃣ Настройки Web Service

**Build & Deploy:**
- **Name:** `vyzhivi-do-zarplaty` (или любое имя)
- **Region:** `Frankfurt (EU Central)` (ближе к России)
- **Branch:** `main`
- **Root Directory:** оставьте пустым
- **Runtime:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `gunicorn app:app`

**Environment Variables (переменные окружения):**

Нажмите **"Add Environment Variable"** и добавьте:

| Key | Value |
|-----|-------|
| `TELEGRAM_BOT_TOKEN` | `7860319102:AAEF7nhRe_D2af0A9RXFm0JmWgzrin3d97k` |
| `WEBAPP_URL` | `https://ваш-сервис.onrender.com` (заполните после создания) |
| `RUN_BOT` | `true` |
| `PORT` | `8080` |

**Instance Type:**
- Выберите **Free** (бесплатный план)

### 4️⃣ Создать сервис

1. Нажмите **"Create Web Service"**
2. Подождите 5-10 минут пока Render соберёт и запустит приложение
3. Вы увидите URL вашего приложения: `https://ваш-сервис.onrender.com`

### 5️⃣ Обновить WEBAPP_URL

1. Скопируйте URL вашего сервиса (например: `https://vyzhivi-do-zarplaty.onrender.com`)
2. Вернитесь в настройки **Environment Variables**
3. Измените `WEBAPP_URL` на ваш реальный URL
4. Нажмите **"Save Changes"**
5. Render автоматически перезапустит сервис

### 6️⃣ Настроить Telegram бота

1. Откройте Telegram, найдите вашего бота
2. Отправьте команду `/start`
3. Нажмите кнопку "🎮 Играть в 'Выживи до зарплаты'"
4. Игра откроется в Telegram WebApp!

### 7️⃣ Поделиться с друзьями

Отправьте друзьям:
- **Ссылку на бота:** `https://t.me/ваш_бот`
- **Прямую ссылку на игру:** `https://ваш-сервис.onrender.com`

## ⚠️ Важные моменты

### Бесплатный план Render

- ✅ Бесплатно навсегда
- ⚠️ Засыпает после 15 минут неактивности
- ⚠️ Первый запуск после сна занимает ~30 секунд
- ✅ 750 часов работы в месяц (достаточно для игры)

### Если сервис засыпает

Render автоматически разбудит сервис когда кто-то откроет игру. Просто подождите 30 секунд.

### Логи и мониторинг

В панели Render вы можете:
- Смотреть логи (вкладка **Logs**)
- Перезапускать сервис (кнопка **Manual Deploy**)
- Смотреть статистику (вкладка **Metrics**)

## 🔧 Проверка работы

После деплоя проверьте:

1. **Веб-версия:** `https://ваш-сервис.onrender.com`
2. **Health check:** `https://ваш-сервис.onrender.com/health`
3. **Telegram бот:** отправьте `/start` боту

## 📝 Обновление кода

Когда вы внесёте изменения в код:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Render автоматически обновит сервис!

## ❓ Проблемы?

### Ошибка при деплое

1. Проверьте логи в Render (вкладка **Logs**)
2. Убедитесь что все переменные окружения установлены
3. Проверьте что `requirements.txt` содержит все зависимости

### Бот не отвечает

1. Проверьте что `RUN_BOT=true` в переменных окружения
2. Проверьте что `TELEGRAM_BOT_TOKEN` правильный
3. Проверьте логи на наличие ошибок

### Игра не загружается

1. Проверьте что `WEBAPP_URL` указывает на правильный URL
2. Откройте консоль браузера (F12) и посмотрите ошибки
3. Проверьте health check: `https://ваш-сервис.onrender.com/health`

## 🎮 Готово!

Теперь ваша игра доступна всему миру!

Поделитесь ссылкой с друзьями и наслаждайтесь игрой! 🎉
